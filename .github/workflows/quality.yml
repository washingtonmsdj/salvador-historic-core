name: Quality

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Lint
        run: bun run lint:ci

      - name: Validate geospatial contracts
        run: bun run geospatial:validate

      - name: Validate site layout
        run: bun run layout:validate

      - name: Validate metric world scale
        run: bun run world:scale-test

      - name: Validate road surfaces
        run: bun run road:test

      - name: Validate road junctions
        run: bun run road:junction-test

      - name: Validate junction surface planes
        run: bun run road:junction-surface-test

      - name: Validate road junction terrain fit
        run: bun run road:junction-terrain-fit-test

      - name: Validate terrain-safe road grading
        run: bun run road:grading-test

      - name: Validate all road terrain fit
        run: bun run road:terrain-fit-test

      - name: Validate walkable player spawn
        run: bun run player:spawn-test

      - name: Validate third-person player
        run: bun run player:third-person-test

      - name: Validate terrain structure masks
        run: bun run terrain:mask-test

      - name: Validate terrain presentation fidelity
        run: bun run terrain:presentation-test

      - name: Validate terrain cliff projection
        run: bun run terrain:projection-test

      - name: Validate building grounding
        run: bun run building:grounding-test

      - name: Validate landmark GIS fidelity
        run: bun run landmark:fidelity-test

      - name: Build
        run: bun run build
